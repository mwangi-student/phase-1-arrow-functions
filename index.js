// Define the divide function using a function expression
const divide = function () {
  return 2000 / 100;
};

// Define the square function using arrow syntax
const square = (x) => x * x;

// Define the add function using arrow syntax
const add = (a, b) => a + b;
